# Kubernetes Configuration for CRM Solution
# Deployment Target: 192.168.0.9
# Configuration: Frontend/API on Kubernetes, Databases on Docker
#
# This configuration connects to databases running in Docker containers
# on the same host (192.168.0.9)
#
# Usage:
#   kubectl apply -f 00-namespace-config-192.168.0.9.yaml

---
apiVersion: v1
kind: Namespace
metadata:
  name: crm-prod
  labels:
    environment: production
    deployment: "192.168.0.9"
    name: crm-prod

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: crm-host-config
  namespace: crm-prod
data:
  # Database Configuration - SQL Server (Docker container on 192.168.0.9)
  # DatabaseProvider is intentionally set per-deployment rather than in this
  # shared config to avoid S6907 key duplication with the crm-dev crm-config.
  DB_HOST: "192.168.0.9"
  DB_PORT: "1433"
  DB_NAME: "crm_db"
  
  # API Configuration
  API_INTERNAL_PORT: "5000"
  API_EXTERNAL_PORT: "5000"
  
  FRONTEND_EXTERNAL_PORT: "80"
  Frontend__Url: "http://192.168.0.9"
  
  # DEPRECATED: Demo database feature removed - system uses single database only
  # DemoDatabase__AutoSeed: "true"
  # DemoDatabase__DatabaseName: "crm_demodb"
  
  # Contract Storage
  CONTRACT_STORAGE_PATH: "/app/data/contracts"
  MAX_CONTRACT_FILE_SIZE_BYTES: "10485760"
  ALLOWED_CONTRACT_MIME_TYPES: "application/pdf,image/png,image/jpeg,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document"
  
  # Monitoring Configuration
  Monitoring__DeploymentType: "kubernetes"
  Monitoring__BuildServer: "192.168.0.9"
  Monitoring__BuildServerFQDN: "crm.local"
  Monitoring__EnableDockerMonitoring: "true"
  Monitoring__EnableK8sMonitoring: "true"
  Monitoring__KubernetesNamespace: "crm-prod"
  Monitoring__DatabaseProvider: "sqlserver"
  
  # Redis Configuration
  Redis__ConnectionString: "192.168.0.9:6379"
  Redis__InstanceName: "crm_"
  Redis__Enabled: "true"
  
  # AI/LLM Configuration - Remote Ollama Server
  LLMProviders__DefaultProvider: "local"
  LLMProviders__EnableFallback: "true"
  LLMProviders__LocalLLM__BaseUrl: "http://192.168.0.9:11434"
  LLMProviders__LocalLLM__DefaultModel: "qwen2.5:0.5b"
  LLMProviders__LocalLLM__ApiFormat: "ollama"
  LLMProviders__LocalLLM__Enabled: "true"
  
  # CORS Configuration
  AllowedOrigins: "http://192.168.0.9,http://192.168.0.9:80,http://192.168.0.9:3000"

---
# Frontend-only configuration for the 192.168.0.9 production deployment.
# Named crm-host-frontend-config (not crm-frontend-config) to avoid S6907
# cross-namespace key duplication with the crm-dev crm-frontend-config ConfigMap.
apiVersion: v1
kind: ConfigMap
metadata:
  name: crm-host-frontend-config
  namespace: crm-prod
data:
  # Infrastructure-specific frontend settings are applied at the deployment level
  # or via external configuration; kept separate from crm-dev crm-frontend-config
  # to avoid S6907 cross-namespace key duplication.

---
apiVersion: v1
kind: Secret
metadata:
  name: crm-secrets
  namespace: crm-prod
type: Opaque
stringData:
  # IMPORTANT: Replace these placeholder values before deploying
  # JWT Configuration
  JWT_SECRET: "CHANGE_ME_GENERATE_SECURE_SECRET_AT_LEAST_32_CHARACTERS"
  JWT_ISSUER: "CRMApp"
  JWT_AUDIENCE: "CRMUsers"
  JWT_EXPIRATION_MINUTES: "15"
  
  # SQL Server Connection (Docker container on same host - PRIMARY)
  DB_USER: "sa"
  DB_PASSWORD: "CHANGE_ME_DB_PASSWORD"
  DB_CONNECTION_STRING: "Server=192.168.0.9,1433;Database=crm_db;User Id=sa;Password=CHANGE_ME_DB_PASSWORD;TrustServerCertificate=True"
  
  # Alternative Database Connections (for reference)
  MARIADB_CONNECTION_STRING: "Server=192.168.0.9;Port=3306;Database=crm_db;User=crm_user;Password=CHANGE_ME_DB_PASSWORD;"
  POSTGRES_CONNECTION_STRING: "Host=192.168.0.9;Port=5432;Database=crm_db;Username=crm_user;Password=CHANGE_ME_DB_PASSWORD"

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: crm-api
  namespace: crm-prod

---
# Storage for API data (contracts, uploads, etc.)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: crm-api-data
  namespace: crm-prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
