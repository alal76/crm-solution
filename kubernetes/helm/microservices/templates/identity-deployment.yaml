{{- if .Values.services.identity.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "crm-microservices.fullname" . }}-identity
  labels:
    {{- include "crm-microservices.labels" . | nindent 4 }}
    app.kubernetes.io/component: identity
spec:
  replicas: {{ .Values.services.identity.replicaCount }}
  selector:
    matchLabels:
      {{- include "crm-microservices.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: identity
  template:
    metadata:
      labels:
        {{- include "crm-microservices.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: identity
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: crm-identity
        image: {{ .Values.images.identity.repository }}:{{ .Values.images.identity.tag }}
        imagePullPolicy: {{ .Values.images.identity.pullPolicy }}
        ports:
        - containerPort: {{ .Values.services.identity.port }}
          name: http
        envFrom:
        - configMapRef:
            name: {{ include "crm-microservices.fullname" . }}-config
        - secretRef:
            name: {{ include "crm-microservices.fullname" . }}-secrets
        env:
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: {{ include "crm-microservices.fullname" . }}-secrets
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "crm-microservices.fullname" . }}-secrets
              key: DB_PASSWORD
        - name: ConnectionStrings__DefaultConnection
          value: "Server=$(DB_HOST);Port=$(DB_PORT);Database=$(DB_NAME);User=$(DB_USER);Password=$(DB_PASSWORD);CharSet=utf8mb4;"
        - name: JwtSettings__Key
          valueFrom:
            secretKeyRef:
              name: {{ include "crm-microservices.fullname" . }}-secrets
              key: JWT_KEY
        - name: JwtSettings__Issuer
          valueFrom:
            configMapKeyRef:
              name: {{ include "crm-microservices.fullname" . }}-config
              key: JWT_ISSUER
        - name: JwtSettings__Audience
          valueFrom:
            configMapKeyRef:
              name: {{ include "crm-microservices.fullname" . }}-config
              key: JWT_AUDIENCE
        resources:
          {{- toYaml (default .Values.resources.default .Values.resources.identity) | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.identity.port }}
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.identity.port }}
          initialDelaySeconds: 10
          periodSeconds: 10
{{- end }}
