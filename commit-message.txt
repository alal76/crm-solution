fix: v0.615.9 - CDT profile save/load and connection test fixes

- Profile save: ssh_key, ssh_password, secrets now included in saved profile
- applyTemplateToWizard: platform mapping uses data.platform fallback; config.target populated; SSH password restored to form; on_premises added to platform map
- _loadAndApplyProfile: merges wizard_config before template apply so all credentials restored
- applySelectedProfile: populates Discovery form fields and secret fields on load
- test_connection: SSH/cloud failure returns HTTP 200 with status=failed instead of HTTP 400
